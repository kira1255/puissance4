/**
 * Represents a player
 */
export interface Player{
    /**
     * player id ( 0 for player one, 1 for player 2)
     */
    id: number;

    /**
     * player name (not used for now)
     */
    name: string;
}